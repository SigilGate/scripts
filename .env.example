# Путь к локальному хранилищу данных (клон приватного репозитория)
SIGIL_STORE_PATH=<path>

# SSH-доступ к Entry-нодам
SIGIL_SSH_KEY=<path>
SIGIL_SSH_USER=<username>
SIGIL_SSH_PASSWORD=<password>

# core/rotate-path.sh (необязательные, используются значения по умолчанию)
# SIGIL_CORE_IP=<ip>                                     # IP этой Core-ноды (фильтр маршрутов)
# SIGIL_CORE_XRAY_CONF=/usr/local/etc/xray/config.json   # Xray-конфиг Core
# SIGIL_CORE_NGINX_DIR=/etc/nginx/sites-enabled           # Nginx sites-enabled
# SIGIL_ENTRY_XRAY_CONF=/usr/local/etc/xray/config.json  # Xray-конфиг Entry
